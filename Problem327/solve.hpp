
#pragma once

/**
 * Returns the number of cards pulled from the dispensor, in order to carry K cards through R doors, carrying at most C cards at a time.
 */
unsigned long solve(unsigned long C, unsigned long R, unsigned long K = 1);
